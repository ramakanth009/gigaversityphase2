import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles(() => ({
    logoimg:{
        height:"40px",
        padding:"10px",
        borderRadius:"50px"
    },
    mainbox:{
        display:"flex",
        justifyContent:"space-between"
    }
}))